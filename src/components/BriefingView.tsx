import { useMemo, useState } from 'react';
import { generateBriefing } from '../briefing/generateBriefing';
import { useAgymStore } from '../state/store';
function daysAgo(days:number){ const d=new Date(); d.setDate(d.getDate()-days); return d.toISOString().slice(0,10); }
function download(name:string, content:string, type='text/markdown'){ const url=URL.createObjectURL(new Blob([content],{type})); const a=document.createElement('a'); a.href=url; a.download=name; a.click(); URL.revokeObjectURL(url); }
export function BriefingView(){ const events=useAgymStore(s=>s.events); const [from,setFrom]=useState(daysAgo(13)); const [to,setTo]=useState(new Date().toISOString().slice(0,10)); const markdown=useMemo(()=>generateBriefing(events,{from,to,generatedAt:new Date().toISOString()}),[events,from,to]); const json=useMemo(()=>JSON.stringify({schemaVersion:1, generatedAt:new Date().toISOString(), range:{from,to}, events: events.filter(e=>e.date>=from&&e.date<=to)},null,2),[events,from,to]); return <section className="panel"><div className="split-head"><div><div className="eyebrow">CONFIRMED CONTEXT THAT TRAVELS</div><h2>Coach Briefing</h2></div><div className="row"><label>From <input type="date" value={from} onChange={e=>setFrom(e.target.value)}/></label><label>To <input type="date" value={to} onChange={e=>setTo(e.target.value)}/></label></div></div><div className="row"><button onClick={()=>navigator.clipboard?.writeText(markdown)}>Copy markdown</button><button className="ghost" onClick={()=>download(`agym-briefing-${from}-to-${to}.md`, markdown)}>Download .md</button><button className="ghost" onClick={()=>download(`agym-context-${from}-to-${to}.json`, json, 'application/json')}>Download JSON</button></div><pre className="briefing">{markdown}</pre></section> }
