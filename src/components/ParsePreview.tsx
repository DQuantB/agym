import { useAgymStore } from '../state/store';
import { EventEditor } from './EventEditor';
export function ParsePreview(){ const drafts=useAgymStore(s=>s.drafts); const confirmAll=useAgymStore(s=>s.confirmAll); const discardAll=useAgymStore(s=>s.discardAll); if(!drafts.length) return <section className="panel"><h2>Reviewed draft</h2><p>No active parsed draft. Submit a raw log to create editable events.</p></section>; return <section className="panel collision"><div className="split-head"><div><div className="eyebrow">USER REVIEW REQUIRED</div><h2>Parsed JSON draft</h2></div><div className="row"><button onClick={confirmAll}>Confirm all</button><button className="ghost" onClick={discardAll}>Discard all</button></div></div>{drafts.map(d=><EventEditor key={d.id} draft={d}/>)}</section> }
